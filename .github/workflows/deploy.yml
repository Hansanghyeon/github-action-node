name: 🚧 github action meta
on: [push, pull_request]

jobs:
  asset:
    name: 🚚 Asset download and host upload
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: 🔨 Node Build release Theme file
      id: test
      uses: actions/setup-node@v2
      with:
        node-vsersion: '16.x'
    - run: cd Theme; yarn install; yarn lint:js; echo '::set-output name=JS::$(yarn lint:js)'
    - name: if fail
      uses: actions/github-script@v4
      env:
        JS_LOG: ${{steps.test.outputs.JS}}
      with:
        script: |
          const { JS_LOG } = process.env
          const ref = "${{github.ref}}"
          const pull_number = Number(ref.split("/")[2])
          console.log(JS_LOG)
          await github.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: JS_LOG
          })
#       if: failure()
